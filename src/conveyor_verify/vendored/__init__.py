"""Vendored copy of conveyor.integrity (canon, hashing, keys, merkle, envelope, chain).

Kept byte-identical to the core by the core repository's `scripts/sync-vendored.sh`;
`tests/test_vendored_sync.py` in the core repo fails when they drift.
"""
